module.exports = {
    name: 'ping',
    description: 'Pong!',
    //devOnly: bool,
    //testOnly: bool,
    //options: Object[],
    //deleted : bool,

    callback: async (client,interaction) =>{
    }
}